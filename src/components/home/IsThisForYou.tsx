import SectionPlaceholder from "./SectionPlaceholder";

export default function IsThisForYou() {
  return (
    <SectionPlaceholder
      title="Is this for You?"
      description="Clueless about points, confusing transfers, scattered points, unclear trip value."
    />
  );
}
