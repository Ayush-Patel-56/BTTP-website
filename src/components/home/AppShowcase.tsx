import SectionPlaceholder from "./SectionPlaceholder";

export default function AppShowcase() {
  return (
    <SectionPlaceholder
      id="try-now"
      title="Check out App"
      description="App preview and download / try-now call to action."
    />
  );
}
